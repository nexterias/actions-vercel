---
layout: doc
title: build-env
description: Configures environment variables for builds run on Vercel, or during vercel build when prebuilt is enabled.
---

# `build-env`

This option specifies environment variables to set when building on Vercel.

::: tip
If [prebuilt](/references/inputs/optional/prebuilt) is enabled, set environment variables when `vercel build` runs.
:::

## Usage example

```yaml {31-33} file="/snippets/references/inputs/build-env.yml"

```
